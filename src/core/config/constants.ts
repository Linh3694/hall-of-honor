/**
 * Hằng số UI / animation dùng chung (tránh magic number rải rác)
 * — có thể mở rộng theo Hall of Honor.
 */

/** Ngưỡng scroll (px) — pattern GSAP / reveal */
export const SCROLL_REVEAL_THRESHOLD_PX = 250;

/** Độ dài truncate mô tả (ký tự) — chỉnh theo design */
export const DESCRIPTION_TRUNCATE_CHARS = 470;

/** Carousel / ticker — ms */
export const CAROUSEL_INTERVAL_MS = 3000;
export const MARQUEE_TICK_MS = 2000;

/** Ảnh cover học bổng / tiểu mục — max-height (px) */
export const SCHOLARSHIP_COVER_MAX_HEIGHT_PX = 470;

/** Swiper đội ngũ — transition speed (ms), khác interval autoplay */
export const HOMEPAGE_SWIPER_SPEED_MS = 2000;
